# ── Server ──────────────────────────────────────────────
PORT=3001

# ── Resend (email) ──────────────────────────────────────
# Sign up free at https://resend.com → API Keys → Create API Key
RESEND_API_KEY=re_25wCQkSY_ERHUzqm8twGG7LpguHxPKLCG

# The "From" address on sent emails.
# Without a verified domain: leave this commented out (uses onboarding@resend.dev).
# After verifying your domain in Resend dashboard, set this to your address:
# RESEND_FROM=VIHelp <hello@vihelp.bt>

# ── Email recipients ────────────────────────────────────
# Where contact form submissions are forwarded
NOTIFY_EMAIL=vihelp.ai@gmail.com

# ── Admin dashboard ─────────────────────────────────────
# Password to log in at http://localhost:3001/admin
ADMIN_PASSWORD=change_this_to_something_strong

# ── JWT ─────────────────────────────────────────────────
JWT_SECRET=change_this_to_a_long_random_string_at_least_32_chars

# ── Frontend URL (for CORS) ──────────────────────────────
FRONTEND_URL=http://localhost:5173
